services:
  - type: web
    name: spec-home-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
